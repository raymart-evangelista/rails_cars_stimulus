<h1><%= @car.name %></h1>

<h1>$<%= @car.price %></h1>

<ul>
  <% if @car.variants.any? %>
    <h1>Variants</h1>
    <li>
    <% @car.vairants.each do |variant| %>
      <h2><%= variant.name %></h2>
      <h2><%= variant.price %></h2>
    <% end %>
    </li>
  <% end %>
</ul>

<div>
</div>

<ul>
  <li><%= link_to "Edit", edit_car_path(@car) %></li>
  <li><%= link_to "Destory", car_path(@car), data: {
    turbo_method: :delete,
    turbo_confirm: "Are you sure?"
  } %></li>
</ul>

<h2>Variants</h2>
<%#= render @article.comments %>
